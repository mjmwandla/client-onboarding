import { C } from '../constants/colors';
import { Cd, Btn, Badge } from '../components';

export function DocUpload({ uploadState, setUploadState, ob, nav, tt }) {
  const up=uploadState,sU=setUploadState; return <div><div style={{textAlign:'center',marginBottom:32}}><div style={{fontSize:24,color:C.tx}}>Secure Document Submission Portal</div><div style={{fontSize:13,color:C.t2,marginTop:4}}>For: <strong style={{color:C.tx}}>{ob?.clientName}</strong></div></div><div style={{maxWidth:640,margin:'0 auto'}}>{[{k:'fatca',n:'FATCA / CRS Self-Certification',f:'fatca_crs_form.pdf',sz:'892KB'},{k:'pfa',n:'PFA Compliance Certificate',f:'pfa_cert_2024.pdf',sz:'344KB'}].map(it=><Cd key={it.k} style={{padding:24,marginBottom:16,borderLeft:`3px solid ${up[it.k]?C.ok:C.wa}`}}><div style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}><div style={{display:'flex',alignItems:'center',gap:12}}><span style={{fontSize:20}}>📎</span><div><div style={{fontSize:14,color:C.tx,fontWeight:500}}>{it.n}</div><div style={{fontSize:11,color:C.t2,marginTop:2}}>PDF, JPG, PNG (max 10MB)</div></div></div><Badge s={up[it.k]?'VERIFIED':'PENDING'}/></div>{up[it.k]?<div style={{marginTop:12,padding:'8px 12px',background:C.el,borderRadius:6,fontSize:12,color:C.ok}}>✅ {it.f} ({it.sz})</div>:<div onClick={()=>sU(p=>({...p,[it.k]:true}))} style={{marginTop:12,padding:24,border:`2px dashed ${C.b2}`,borderRadius:8,textAlign:'center',cursor:'pointer',color:C.t2,fontSize:13}}>Drag file here or click to browse</div>}</Cd>)}<div style={{textAlign:'center',marginTop:20}}><Btn primary onClick={()=>{tt('Documents submitted');nav('FICA_VERIFICATION')}}>Submit Documents</Btn></div></div></div>;
}
